services:

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    networks:
      - backend

  app:
    build: ./app
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
    networks:
      - backend

  nginx:
    build: ./nginx
    ports:
      - "80:80"
    depends_on:
      - app
    networks:
      - backend

volumes:
  redis_data:

networks:
  backend:
